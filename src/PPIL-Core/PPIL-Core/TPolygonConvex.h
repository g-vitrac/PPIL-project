#pragma once
#include "PolygonConvex.h"
#include "Vecteur2D.h"

class TPolygonConvex {
public:

	inline void test() {
		/*
		Vecteur2D A(2, 2), B(2, -2), C(-2, -2), D(-2, 2);
		vector<Vecteur2D> vec;
		vec.push_back(A);
		vec.push_back(B);
		vec.push_back(C);
		vec.push_back(D);

		PolygonConvex p(vec, Form::BLACK);
		cout << p << endl;
		*/
	}
};
