#pragma once
#include "PolygonConvex.h"
#include "PolygonRegular.h"

int main() {
	Vector2D A(2, 2), B(2, -2), C(-2, -2), D(-2, 2);
	vector<Vector2D> vec;
	vec.push_back(A);
	vec.push_back(B);
	vec.push_back(C);
	vec.push_back(D);
}